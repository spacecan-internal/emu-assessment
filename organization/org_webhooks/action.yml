name: 'Organization GitHub Webhooks'
description: 'Get Organization GitHub Webhooks'

inputs:
  org:
    description: 'Organization name'
    required: true

runs:
  using: "composite"
  steps:
    - run: ./script.sh ${{ inputs.org }}
      shell: bash
      working-directory: ./organization/org_webhooks
